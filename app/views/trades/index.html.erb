  <ul class="breadcrumb">
    <li><a href="/">Inicio</a></li>
    <li><a href="/trades">Empresas</a></li>
    <li class="active">Lista</li>
  </ul>
  <%= render 'shared/flash.html.erb' %>
  <h2 class="title-panel"><%= @title_main %></h2>
  <div class="button-rigth-action">
    <%= link_to 'Nueva empresa', new_trade_path, { :class=>"btn btn-primary" } %>
  </div>
  <table class="table table-striped table-hover ">
    <thead>
    <tr>
      <th>Nombre</th>
      <th class="table-center-text">Opciones</th>
    </tr>
    </thead>
    <tbody>
    <tr>
  <tbody>
    <% @trades.each do |trade| %>
      <tr>
        <td><%= trade.name %></td>
        <td width="140px">
          <div class="btn-group">
              <span class="btn btn-default btn-sm">Seleccione</span>
                <a href="#" class="btn btn-primary btn-sm dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      <li><%= link_to 'Mostrar', trade %></li>
                      <li><%= link_to 'Editar', edit_trade_path(trade) %></li>
                      <li><%= link_to 'Eliminar',trade, method: :delete, data: { confirm: 'Está seguro que desea eliminar la empresa?' } %></li>
                  </ul>
            </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>