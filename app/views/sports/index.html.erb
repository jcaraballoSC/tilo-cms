  <ul class="breadcrumb">
    <li><a href="/">Inicio</a></li>
    <li><a href="/sports">Deportes</a></li>
    <li class="active">Lista</li>
  </ul>
  <%= render 'shared/flash.html.erb' %>
  <h2 class="title-panel"><%= @title_main %></h2>
  <div class="button-rigth-action">
    <%= link_to 'Nuevo deporte', new_sport_path, { :class=>"btn btn-primary" } %>
  </div>
  <table class="table table-striped table-hover ">
    <thead>
    <tr>
      <th>Url</th>
      <th>Fecha</th>
      <th class="table-center-text">Opciones</th>
    </tr>
    </thead>
    <tbody>
    <tr>
  <tbody>
    <% @sports.each do |sport| %>
      <tr>
        <td><%= sport.url %></td>
        <td><%= sport.date %></td>
        <td width="140px">
          <div class="btn-group">
              <span class="btn btn-default btn-sm">Seleccione</span>
                <a href="#" class="btn btn-primary btn-sm dropdown-toggle" data-toggle="dropdown"><span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      <li><%= link_to 'Mostrar', sport %></li>
                      <li><%= link_to 'Editar', edit_user_path(sport) %></li>
                      <li><%= link_to 'Eliminar',sport, method: :delete, data: { confirm: 'Está seguro que desea eliminar la url?' } %></li>
                  </ul>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>